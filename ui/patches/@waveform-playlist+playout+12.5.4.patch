diff --git a/node_modules/@waveform-playlist/playout/dist/index.d.mts b/node_modules/@waveform-playlist/playout/dist/index.d.mts
index 1a870e2..1bca8c1 100644
--- a/node_modules/@waveform-playlist/playout/dist/index.d.mts
+++ b/node_modules/@waveform-playlist/playout/dist/index.d.mts
@@ -372,7 +372,7 @@ interface SoundFontToneTrackOptions {
     clips: MidiClipInfo[];
     track: Track;
     soundFontCache: SoundFontCache;
-    /** GM program number (0-127) for melodic instruments */
+    /** SoundFont preset number (0-127), including a drum-kit preset on channel 10 */
     programNumber?: number;
     /** Whether this track uses percussion bank (channel 9) */
     isPercussion?: boolean;
@@ -410,7 +410,7 @@ declare class SoundFontToneTrack implements PlayableTrack {
     /**
      * Trigger a note by creating a native AudioBufferSourceNode from the SoundFont cache.
      *
-     * Per-note routing: channel 9 → bank 128 (drums), others → bank 0 with programNumber.
+     * Per-note routing: channel 9 → bank 128 (drums), others → bank 0; both use programNumber.
      */
     private triggerNote;
     /**
diff --git a/node_modules/@waveform-playlist/playout/dist/index.d.ts b/node_modules/@waveform-playlist/playout/dist/index.d.ts
index 1a870e2..1bca8c1 100644
--- a/node_modules/@waveform-playlist/playout/dist/index.d.ts
+++ b/node_modules/@waveform-playlist/playout/dist/index.d.ts
@@ -372,7 +372,7 @@ interface SoundFontToneTrackOptions {
     clips: MidiClipInfo[];
     track: Track;
     soundFontCache: SoundFontCache;
-    /** GM program number (0-127) for melodic instruments */
+    /** SoundFont preset number (0-127), including a drum-kit preset on channel 10 */
     programNumber?: number;
     /** Whether this track uses percussion bank (channel 9) */
     isPercussion?: boolean;
@@ -410,7 +410,7 @@ declare class SoundFontToneTrack implements PlayableTrack {
     /**
      * Trigger a note by creating a native AudioBufferSourceNode from the SoundFont cache.
      *
-     * Per-note routing: channel 9 → bank 128 (drums), others → bank 0 with programNumber.
+     * Per-note routing: channel 9 → bank 128 (drums), others → bank 0; both use programNumber.
      */
     private triggerNote;
     /**
diff --git a/node_modules/@waveform-playlist/playout/dist/index.js b/node_modules/@waveform-playlist/playout/dist/index.js
index cb70a42..70d0313 100644
--- a/node_modules/@waveform-playlist/playout/dist/index.js
+++ b/node_modules/@waveform-playlist/playout/dist/index.js
@@ -946,7 +946,10 @@ var SoundFontToneTrack = class {
    */
   triggerNote(midiNote, duration, time, velocity, channel) {
     const bank = channel === 9 ? 128 : this.bankNumber;
-    const preset = channel === 9 ? 0 : this.programNumber;
+    // Channel 10 still selects the drum bank, but the selected drum-kit
+    // preset must remain configurable (FluidR3 exposes several kits in bank
+    // 128).  `programNumber` is supplied from the MIDI clip by the provider.
+    const preset = this.programNumber;
     const sfSample = this.soundFontCache.getAudioBuffer(midiNote, bank, preset);
     if (!sfSample) {
       if (!this._missingSampleWarned) {
diff --git a/node_modules/@waveform-playlist/playout/dist/index.mjs b/node_modules/@waveform-playlist/playout/dist/index.mjs
index 5b65796..08c81df 100644
--- a/node_modules/@waveform-playlist/playout/dist/index.mjs
+++ b/node_modules/@waveform-playlist/playout/dist/index.mjs
@@ -923,7 +923,10 @@ var SoundFontToneTrack = class {
    */
   triggerNote(midiNote, duration, time, velocity, channel) {
     const bank = channel === 9 ? 128 : this.bankNumber;
-    const preset = channel === 9 ? 0 : this.programNumber;
+    // Channel 10 still selects the drum bank, but the selected drum-kit
+    // preset must remain configurable (FluidR3 exposes several kits in bank
+    // 128).  `programNumber` is supplied from the MIDI clip by the provider.
+    const preset = this.programNumber;
     const sfSample = this.soundFontCache.getAudioBuffer(midiNote, bank, preset);
     if (!sfSample) {
       if (!this._missingSampleWarned) {
